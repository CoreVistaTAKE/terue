<!-- config/fonts.conf (例) -->
<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "fonts.dtd">
<fontconfig>
  <!-- Herokuにあるフォントの場所を指定する -->
  <dir>/app/.apt/usr/share/fonts</dir>

  <!-- キャッシュを置く場所(これでOK) -->
  <cachedir>/app/.apt/var/cache/fontconfig</cachedir>
</fontconfig>